{
  "private": true,
  "description": "Here I have implemented a 'To Do' application. The feature of this application is that it is possible to switch case storage: 'localStorage', 'indexedDB', 'some server database'.",
  "scripts": {
    "lint": "eslint ./",
    "lint:fix": "eslint ./ --fix",
    "prettier": "prettier --check  --config ./.prettierrc.js ./**/*.js",
    "prettier:fix": "prettier --write --config ./.prettierrc.js ./**/*.js",
    "start": "node ./server/server.js"
  },
  "devDependencies": {
    "@babel/eslint-parser": "^7.19.1",
    "eslint": "8.26.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-config-prettier": "8.5.0",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "2.7.1"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
